[tool.poetry]
name = "example_library"
version = "0.1.0"
description = "Example python library"
authors = ["Hermit Technology"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"